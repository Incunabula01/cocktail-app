/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['www.thecocktaildb.com']
    },
    video: {
        domains: ['www.thecocktaildb.com']
    }
}

module.exports = nextConfig
